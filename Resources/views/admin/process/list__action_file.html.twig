{% if vich_uploader_asset(object, 'file') is not empty %}
    <a class="btn btn-default btn-sm" href="{{ vich_uploader_asset(object, 'file') }}">
    	<span class='glyphicon glyphicon-download'></span> Fichier
    </a>
{% endif %}