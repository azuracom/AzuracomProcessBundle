{% extends '@SonataAdmin/CRUD/base_list_field.html.twig' %}

{% block field %}
    {% set difference = object.executionDiff %}
    {% if difference %}
        {{ difference.format(field_description.options.format) }}
    {% endif %}
{% endblock %}